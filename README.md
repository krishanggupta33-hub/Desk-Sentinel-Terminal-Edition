It is a typing game 

Codes fall from the top of the screen. You type the code before it hits the bottom or your server takes damage. Clear enough blocks and it speeds up / spawns faster. Health hits 0, game over. The game is based on python.

Install & run (Windows)

This is a desktop game

Download the release from Github 

Unzip the file and open
"Windows protected your PC" (blue screen) — click More info → Run anyway. This one has a bypass built in.




Only needed if you want to run from source instead of the exe above.

This is a desktop game, not a web game. You need Python installed once, then it just runs.

If Smartapp Control Blocks it then:


Install Python from https://python.org/downloads if you don't have it. On the installer's first screen, tick "Add python.exe to PATH" before clicking Install — easy to miss, breaks everything below if skipped.
Open Command Prompt.
Install the one dependency:


  " pip install pygame"

If pip isn't recognized, use py -m pip install pygame instead.
4. open cmd into the folder with DeskSentinel.py, then run:

   python DeskSentinel.py

AI used for Debugging and Uploading the release on Github
